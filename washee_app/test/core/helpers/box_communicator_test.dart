import 'package:flutter_test/flutter_test.dart';

void main() {
  group("Unlocking", () {
    test(
      'should perform a POST request on unlock endpoint',
      () async {
        // arrange

        // act

        // assert
      },
    );

    test(
      'should return true when the response code is 200',
      () async {
        // arrange

        // act

        // assert
      },
    );

    test(
      'should return false when the response code is NOT 200',
      () async {
        // arrange

        // act

        // assert
      },
    );
  });

  group(
    "Locking",
    () {
      test(
        'should perform a POST request on lock endpoint',
        () async {
          // arrange

          // act

          // assert
        },
      );

      test(
        'should return true when response code is 200',
        () async {
          // arrange

          // act

          // assert
        },
      );

      test(
        'should return false when response code is NOT 200',
        () async {
          // arrange

          // act

          // assert
        },
      );
    },
  );
}
