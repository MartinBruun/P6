name: Monitor CI

on:
  pull_request:
    paths:
      - 'washee_box/**'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Move into correct folder
      run: cd washee_box
    - name: Build box Docker Image
      run: docker build . --file Dockerfile --tag box
    - name: Run tests
      run: docker run box sh -c "python -m pytest -vv"
