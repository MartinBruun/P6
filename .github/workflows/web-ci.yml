name: Web CI

on:
  pull_request:
    paths:
      - 'washee_web/**'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run tests and see if it can build for production
      working-directory: ./washee_web
      run: |
        docker run
