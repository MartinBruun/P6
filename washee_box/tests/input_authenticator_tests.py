def test_the_user_gets_granted_access_by_valid_token():
    authenticator = InputAuthenticator()
    invalid_user_request = {
        "data": "data"
    }
    
    # How to act on this
    
    assert False    
    
def test_the_user_gets_denied_access_by_invalid_token():
    authenticator = InputAuthenticator()
    invalid_user_request = {
        "data": "data"
    }
    
    # How to act on this
    
    assert False